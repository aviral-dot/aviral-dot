name: Generate Contribution Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Generate contribution snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            github-contribution-grid-snake.svg?color_dots=#0e4429,#006d32,#26a641,#39d353,#7ee787
            github-contribution-grid-snake-dark.svg?color_dots=#0e4429,#006d32,#26a641,#39d353,#7ee787

      - name: Publish snake
        uses: crazy-max/ghaction-github-pages@v4
        with:
          build_dir: .
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BUILD_DIR: .
